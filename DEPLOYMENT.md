# 📋 Чеклист готовности к деплою

## ✅ КРИТИЧЕСКИЕ (обязательно перед деплоем)

- [x] **Секретные данные вынесены из репозитория**
  - [x] .env добавлен в .gitignore
  - [x] Создан .env.example с примерами
  - [x] Все токены и ключи хранятся в .env

- [x] **Настроен .gitignore**
  - [x] node_modules
  - [x] dist/
  - [x] data/
  - [x] logs/
  - [x] .env

- [x] **Docker готов**
  - [x] Dockerfile создан
  - [x] docker-compose.yml настроен
  - [x] Multi-stage build для оптимизации размера
  - [x] Health check настроен

- [x] **Health checks**
  - [x] GET /health endpoint
  - [x] GET /ready endpoint
  - [x] GET /live endpoint

- [x] **Graceful shutdown**
  - [x] SIGTERM обработка
  - [x] SIGINT обработка
  - [x] Завершение текущих операций

## ✅ ВАЖНЫЕ (рекомендуется)

- [x] **Логирование**
  - [x] Winston настроен
  - [x] Ротация логов
  - [x] Разделение по уровням
  - [x] Структурированные логи

- [x] **Конфигурация**
  - [x] Централизованный config service
  - [x] Валидация переменных окружения
  - [x] Типизация конфигурации

- [x] **PM2 конфигурация**
  - [x] ecosystem.config.cjs создан
  - [x] Автоперезапуск настроен
  - [x] Лимиты памяти установлены

- [x] **CI/CD**
  - [x] GitHub Actions workflow
  - [x] Автоматическая сборка
  - [x] Docker build & push

- [x] **Документация**
  - [x] README с инструкциями
  - [x] Примеры запуска
  - [x] Troubleshooting секция

## 🔄 ДОПОЛНИТЕЛЬНО (можно добавить позже)

- [ ] **Тесты**
  - [ ] Unit тесты
  - [ ] Integration тесты
  - [ ] E2E тесты

- [ ] **Мониторинг**
  - [ ] Prometheus метрики
  - [ ] Grafana дашборды
  - [ ] Alerting (PagerDuty, Slack)

- [ ] **Безопасность**
  - [ ] Rate limiting
  - [ ] HTTPS
  - [ ] Secrets management (Vault, AWS Secrets Manager)

- [ ] **Performance**
  - [ ] Кеширование
  - [ ] Оптимизация запросов
  - [ ] Профилирование

## 🚀 ИНСТРУКЦИИ ПО ДЕПЛОЮ

### 1. Подготовка сервера

```bash
# Обновление системы
sudo apt update && sudo apt upgrade -y

# Установка Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# Установка Docker Compose
sudo apt install docker-compose-plugin -y

# Установка Node.js (если без Docker)
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs

# Установка PM2 (если без Docker)
sudo npm install -g pm2
```

### 2. Клонирование репозитория

```bash
git clone <your-repo-url>
cd moex-donchian-levels
```

### 3. Настройка окружения

```bash
# Копируем пример .env
cp .env.example .env

# Редактируем .env
nano .env
```

### 4. Выбор метода деплоя

#### Вариант A: Docker (рекомендуется)

```bash
# Сборка и запуск
docker-compose up -d

# Проверка статуса
docker-compose ps

# Логи
docker-compose logs -f bot

# Health check
curl http://localhost:3000/health
```

#### Вариант B: PM2

```bash
# Установка зависимостей
npm ci --only=production

# Сборка
npm run build

# Запуск
pm2 start ecosystem.config.cjs

# Сохранение для автозапуска
pm2 save
pm2 startup
```

#### Вариант C: Systemd

```bash
# Создание service файла
sudo nano /etc/systemd/system/moex-bot.service

# Перезагрузка systemd
sudo systemctl daemon-reload

# Включение автозапуска
sudo systemctl enable moex-bot

# Запуск
sudo systemctl start moex-bot

# Проверка статуса
sudo systemctl status moex-bot
```

### 5. Проверка работоспособности

```bash
# Health check
curl http://localhost:3000/health

# Логи (Docker)
docker-compose logs -f bot

# Логи (PM2)
pm2 logs moex-bot

# Логи (Systemd)
sudo journalctl -u moex-bot -f
```

### 6. Мониторинг

```bash
# Docker stats
docker stats moex-bot

# PM2 monitoring
pm2 monit

# Логи приложения
tail -f logs/application-$(date +%Y-%m-%d).log
```

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Проверьте системное время** - должно быть синхронизировано через NTP
2. **Настройте firewall** - откройте только необходимые порты
3. **Бэкапы** - настройте регулярное резервное копирование директории `data/`
4. **Мониторинг** - настройте алерты на критические ошибки
5. **Обновления** - регулярно обновляйте зависимости

## 📊 МЕТРИКИ ДЛЯ ОТСЛЕЖИВАНИЯ

- Uptime приложения
- Количество обработанных ордеров
- Количество ошибок API
- Использование памяти
- Использование CPU
- Размер логов
- Время ответа health check

## 🆘 КОНТАКТЫ ПОДДЕРЖКИ

При возникновении проблем:
1. Проверьте логи приложения
2. Проверьте health check endpoint
3. Проверьте статус контейнера/процесса
4. Создайте issue в репозитории с описанием проблемы

## 🎉 ГОТОВО!

Если все пункты выполнены, приложение готово к деплою в продакшен.
